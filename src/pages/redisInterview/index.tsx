import React from 'react';
import InterviewLayout, { SectionItem } from '../javaBasics/InterviewLayout';

const sections: SectionItem[] = [
  /* ==================== 1. 基础与数据结构 ==================== */
  {
    title: '基础与数据结构',
    icon: '🗄️',
    qas: [
      {
        question: 'Redis 是什么？为什么这么快？',
        answer: [
          '作用：Redis（Remote Dictionary Server）是基于内存的高性能 Key-Value 数据库，支持丰富的数据结构，广泛用于缓存、分布式锁、消息队列、排行榜等场景。',
          '快的原因：① 纯内存操作，读写速度在微秒级（10w+ QPS）；② 单线程模型避免了上下文切换和竞争开销（6.0 后 IO 多线程，命令执行仍是单线程）；③ I/O 多路复用（epoll），一个线程同时监听大量连接；④ 高效的数据结构（SDS、ziplist、skiplist 等）。',
          '解决的问题：数据库（MySQL）在高并发下扛不住 → Redis 作为缓存层挡在数据库前面，承接热点读请求，将 QPS 从千级提升到十万级。',
        ],
        tags: ['基础', '高频'],
        difficulty: '简单',
      },
      {
        question: 'Redis 有哪些数据结构？分别有什么应用场景？',
        answer: [
          'String：最基础的类型，可存字符串/数字/二进制。应用：缓存对象（JSON 序列化）、计数器（INCR）、分布式锁（SETNX）、分布式 Session。',
          'Hash：键值对集合，适合存储对象的多个字段。应用：用户信息（HSET user:1 name "Tom" age 25）、购物车（商品ID → 数量）。比 String 存 JSON 省内存且支持部分更新。',
          'List：有序列表（双向链表/压缩列表）。应用：消息队列（LPUSH + BRPOP）、最新消息列表（微博 Timeline）、分页查询。',
          'Set：无序不重复集合。应用：标签系统、共同好友（SINTER 交集）、抽奖（SRANDMEMBER）、去重。',
          'ZSet（Sorted Set）：有序不重复集合，每个元素关联一个 score 排序。底层：跳表 + 哈希表。应用：排行榜（ZREVRANGE）、延时队列、滑动窗口限流。',
          '高级类型：Bitmap（签到/布隆过滤器）、HyperLogLog（UV 基数统计，12KB 内存统计亿级数据）、GEO（附近的人）、Stream（消息队列，支持消费者组）。',
        ],
        tags: ['数据结构', '高频'],
        difficulty: '中等',
      },
      {
        question: 'Redis 的底层数据结构有哪些？',
        answer: [
          'SDS（Simple Dynamic String）：Redis 自研字符串，相比 C 字符串：① 记录了长度，O(1) 获取 strlen；② 二进制安全，可存图片等；③ 空间预分配和惰性释放，减少内存分配次数。',
          'ziplist（压缩列表）：紧凑内存存储的连续内存块，节省空间。元素少时 Hash、List、ZSet 底层都用 ziplist。缺点：连锁更新。Redis 7.0 用 listpack 替代。',
          'quicklist：Redis 3.2+ List 的底层实现，ziplist 的双向链表。兼顾内存和性能。',
          'dict（哈希表）：两个 hash 表实现渐进式 rehash。Redis 的核心数据结构，键空间就是一个 dict。',
          'skiplist（跳表）：ZSet 的底层实现之一，查找 O(logN)，实现比红黑树简单，范围查询友好。',
          'intset（整数集合）：元素全是整数且数量少时 Set 的底层实现，紧凑存储。',
        ],
        tags: ['底层实现'],
        difficulty: '困难',
      },
    ],
  },

  /* ==================== 2. 持久化 ==================== */
  {
    title: '持久化机制',
    icon: '💾',
    qas: [
      {
        question: 'RDB 持久化是什么？',
        answer: [
          '作用：RDB（Redis Database）是将某个时间点的全量数据快照保存到磁盘（dump.rdb 文件），类似"拍照"。',
          '触发方式：① save 命令（阻塞主线程，生产禁用）；② bgsave 命令（fork 子进程，利用 COW 机制异步保存）；③ 自动触发（配置 save 900 1 表示 900 秒内有 1 次写操作就触发）。',
          '优点：① 紧凑的二进制文件，恢复速度快（适合灾备）；② 对性能影响小（子进程操作）。',
          '缺点：① 非实时持久化，最后一次快照之后的数据会丢失；② fork 大内存实例时可能阻塞（几百毫秒到秒级）。',
          '解决的问题：提供全量备份能力，适合冷备、数据迁移、灾难恢复场景。',
        ],
        tags: ['RDB', '持久化'],
        difficulty: '中等',
      },
      {
        question: 'AOF 持久化是什么？',
        answer: [
          '作用：AOF（Append Only File）记录每一条写命令到文件，恢复时重新执行所有命令。类似"记日志"。',
          '写回策略：① always（每条命令都 fsync，最安全但性能最差）；② everysec（默认推荐，每秒 fsync，最多丢 1 秒数据）；③ no（由 OS 决定 fsync 时机，性能最好但可能丢数据较多）。',
          'AOF 重写（rewrite）：AOF 文件越来越大 → bgrewriteaof 触发重写，用最少的命令重建当前数据集，大幅缩小文件。',
          '优点：数据安全性高，最多丢 1 秒数据。缺点：文件比 RDB 大，恢复速度慢。',
          'Redis 4.0+ 混合持久化：AOF 重写时将 RDB 数据 + 增量 AOF 写入同一文件。恢复时先加载 RDB 部分（快），再重放 AOF 部分（全）。',
          '解决的问题：弥补 RDB 数据丢失风险，实现近实时持久化，保证极端情况下数据丢失最小化。',
        ],
        tags: ['AOF', '持久化', '高频'],
        difficulty: '中等',
      },
    ],
  },

  /* ==================== 3. 缓存问题 ==================== */
  {
    title: '缓存穿透、击穿、雪崩',
    icon: '⚠️',
    qas: [
      {
        question: '什么是缓存穿透？如何解决？',
        answer: [
          '定义：查询一个缓存和数据库中都不存在的数据，每次请求都穿透到数据库。恶意攻击者可利用这一点打垮数据库。',
          '解决方案一：缓存空值。数据库未命中时，将 key → null 写入缓存并设置较短 TTL（如 5 分钟）。缺点：大量不存在的 key 会占用内存。',
          '解决方案二：布隆过滤器（Bloom Filter）。在缓存之前加一层位图过滤器，先判断 key 是否"可能存在"。不存在则直接拦截，存在才查缓存/数据库。误判率可控（通常 < 1%）。',
          '解决方案三：接口层参数校验，拦截明显非法的请求（如 id < 0）。',
          '解决的问题：防止恶意请求或脏数据大量打到数据库，保护后端存储。',
        ],
        tags: ['缓存穿透', '高频'],
        difficulty: '中等',
      },
      {
        question: '什么是缓存击穿？如何解决？',
        answer: [
          '定义：某个热点 key 在缓存过期的瞬间，大量并发请求同时打到数据库查询该 key，导致数据库瞬间压力剧增。',
          '解决方案一：互斥锁（分布式锁）。缓存未命中时，先获取锁，只让一个线程查数据库并回写缓存，其他线程等待或重试。SETNX + 超时时间实现。',
          '解决方案二：逻辑过期（不设 TTL）。缓存不设过期时间，在 value 中存一个逻辑过期时间。读取时发现逻辑过期 → 异步线程更新缓存，当前请求返回旧数据。',
          '解决方案三：热点 key 预加载 + 续期。监控热点 key，在即将过期时自动续期或提前刷新。',
          '解决的问题：保护数据库不被热点 key 过期瞬间的并发洪峰打垮。',
        ],
        tags: ['缓存击穿', '高频'],
        difficulty: '中等',
      },
      {
        question: '什么是缓存雪崩？如何解决？',
        answer: [
          '定义：大量缓存 key 在同一时间过期，或者 Redis 宕机，导致所有请求直接打到数据库，引发数据库崩溃。',
          '针对大量 key 同时过期：① 设置随机过期时间（在基础 TTL 上加随机值，错开过期时间）；② 互斥锁控制数据库查询并发。',
          '针对 Redis 宕机：① Redis 高可用部署（哨兵 / Cluster 集群）；② 多级缓存架构（本地缓存 Caffeine + Redis + 数据库）；③ 限流降级（Sentinel），数据库扛不住时熔断，返回兜底数据。',
          '解决的问题：防止缓存层集体失效导致数据库被流量洪峰冲垮，保证系统在极端情况下仍可降级服务。',
        ],
        tags: ['缓存雪崩', '高频'],
        difficulty: '中等',
      },
    ],
  },

  /* ==================== 4. 分布式锁与高可用 ==================== */
  {
    title: '分布式锁与高可用',
    icon: '🔒',
    qas: [
      {
        question: 'Redis 如何实现分布式锁？有哪些注意点？',
        answer: [
          '基本实现：SET key value NX EX timeout（原子操作：不存在时设置 + 设置过期时间）。',
          '注意点一：value 必须唯一（如 UUID + 线程 ID），释放锁时先比较再删除（Lua 脚本保证原子性），防止误删别人的锁。',
          '注意点二：过期时间要合理。业务执行超过锁超时 → 锁被自动释放 → 其他线程获取锁 → 并发问题。解决：看门狗机制（Redisson 自动续期）。',
          '注意点三：主从复制的 Redis 不可靠（Master 加锁后未同步到 Slave 就宕机 → Slave 提升为 Master → 锁丢失）。解决：RedLock 算法（向 N 个独立 Redis 节点加锁，过半成功才算获取锁）。',
          'Redisson 框架封装了以上所有细节：可重入锁、公平锁、联锁、红锁、读写锁，生产环境直接使用 Redisson。',
          '解决的问题：分布式环境下多个实例竞争同一资源（如扣库存、抢购），需要一个全局互斥机制来保证同一时刻只有一个实例执行关键操作。',
        ],
        tags: ['分布式锁', '高频'],
        difficulty: '困难',
      },
      {
        question: 'Redis 哨兵（Sentinel）模式是什么？',
        answer: [
          '作用：实现 Redis 主从架构的高可用。当 Master 宕机时，Sentinel 自动将 Slave 提升为新 Master，并通知客户端切换连接。',
          '核心功能：① 监控（定期 ping 检测节点健康）；② 通知（故障时通知客户端和其他 Sentinel）；③ 自动故障转移（选举新 Master）；④ 配置中心（客户端向 Sentinel 询问当前 Master 地址）。',
          '故障判定：主观下线（单个 Sentinel 认为节点不可达）→ 客观下线（超过配置数量的 Sentinel 达成共识）→ Sentinel 之间选举 Leader → Leader 执行故障转移。',
          '解决的问题：手动故障转移耗时且容易出错 → 自动化高可用，故障恢复时间从"分钟级人工操作"缩短到"秒级自动切换"。',
        ],
        tags: ['哨兵', '高可用'],
        difficulty: '中等',
      },
      {
        question: 'Redis Cluster 集群是什么？',
        answer: [
          '作用：Redis 官方的分布式集群方案，实现数据分片 + 高可用，突破单机内存和性能上限。',
          '数据分片：16384 个哈希槽（Hash Slot），通过 CRC16(key) % 16384 确定 key 归属于哪个节点。每个节点负责一部分槽。',
          '高可用：每个 Master 节点有 1 个或多个 Slave 节点。Master 宕机时，集群内部通过 Gossip 协议选举 Slave 接替。',
          '客户端路由：客户端向任意节点发送命令，如果 key 不在该节点 → 返回 MOVED 重定向（或 ASK，正在迁移中），客户端重新请求正确节点。Smart Client（如 Lettuce）会缓存槽映射关系。',
          '限制：不支持跨槽的多 key 操作（如 MGET 的 key 不在同一个槽会报错），可通过 Hash Tag（{prefix}.key）强制同槽。',
          '解决的问题：单机 Redis 内存有限（通常 10-32GB）→ 集群可横向扩展到 TB 级；单机 QPS 有上限 → 多节点分摊请求。',
        ],
        tags: ['Cluster', '分片'],
        difficulty: '困难',
      },
    ],
  },

  /* ==================== 5. 过期与淘汰策略 ==================== */
  {
    title: '过期与内存淘汰策略',
    icon: '🧹',
    qas: [
      {
        question: 'Redis 的过期删除策略是什么？',
        answer: [
          '惰性删除：访问 key 时才检查是否过期，过期则删除。优点：不浪费 CPU。缺点：大量过期 key 不被访问就一直占内存。',
          '定期删除：Redis 每隔 100ms 随机抽取一部分设置了过期时间的 key 检查，过期则删除。如果过期 key 比例高则继续抽取（自适应频率）。',
          '两者配合使用：定期删除处理大部分过期 key，惰性删除作为兜底，尽量减少内存浪费。',
          '仍有遗漏：部分过期 key 既没被访问也没被定期删除抽到 → 需要内存淘汰策略兜底。',
        ],
        tags: ['过期策略'],
        difficulty: '中等',
      },
      {
        question: 'Redis 的内存淘汰策略有哪些？',
        answer: [
          '当内存达到 maxmemory 上限时触发淘汰。8 种策略：',
          'noeviction（默认）：不淘汰，新写入直接报错。适合不允许丢数据的场景。',
          'allkeys-lru：在所有 key 中，淘汰最近最少使用的。最常用，适合缓存场景。',
          'allkeys-lfu（Redis 4.0+）：在所有 key 中，淘汰访问频率最低的。比 LRU 更精准，适合有热点数据的场景。',
          'allkeys-random：在所有 key 中随机淘汰。',
          'volatile-lru：只在设置了过期时间的 key 中，淘汰最近最少使用的。',
          'volatile-lfu：只在设置了过期时间的 key 中，淘汰访问频率最低的。',
          'volatile-random：在设置了过期时间的 key 中随机淘汰。',
          'volatile-ttl：淘汰剩余存活时间最短的 key。',
          '解决的问题：Redis 内存有限，当数据量超过内存上限时，需要一种自动淘汰机制保证 Redis 可用，同时尽量保留"有价值"的热数据。',
        ],
        tags: ['淘汰策略', '高频'],
        difficulty: '中等',
      },
    ],
  },
];

const RedisInterviewPage: React.FC = () => (
  <InterviewLayout
    title="Redis 面试题"
    description="覆盖数据结构、持久化、缓存三连击、分布式锁、高可用集群与内存淘汰策略。"
    sections={sections}
  />
);

export default RedisInterviewPage;
